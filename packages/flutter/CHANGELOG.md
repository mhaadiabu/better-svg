# Changelog

## 0.1.0

- Initial release. Inline SVG rendering for Flutter using `flutter_svg`. Supports `src`/`name`, `data:` URLs, in-memory cache, sanitization, loading/fallback states, and a custom HTTP client.
