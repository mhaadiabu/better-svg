# Implementation Plans

Generated by the improve skill on 2026-06-23 against commit `7e1b3b6`. Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row when
done — unless a reviewer dispatched you and told you they maintain the index.

## Execution order & status

| Plan | Title                                                              | Priority | Effort | Depends on   | Status |
|------|--------------------------------------------------------------------|----------|--------|--------------|--------|
| 001  | Add vitest harness + core characterization tests                   | P1       | M      | —            | DONE   |
| 002  | Unify DOM sanitization across React/Vue/Svelte                     | P1       | M      | 001          | DONE   |
| 003  | Fix React SVG cache write                                          | P1       | S      | 001          | DONE   |
| 004  | Fix Flutter sanitizer (discarded result + close-tag leak)          | P1       | S      | 001          | TODO   |
| 005  | Run lint+test in CI; remove unused import                          | P2       | S      | 001          | TODO   |
| 006  | Stabilize React/Native effects against inline callbacks            | P2       | S      | 001, 003     | TODO   |
| 007  | Remove dead @better-svg/ui scaffolding                             | P3       | S      | —            | TODO   |
| 008  | Docs consistency pass + remove false promises                      | P2       | M      | 002,003,004,006 | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **001 is the foundation.** It installs vitest + happy-dom + @testing-library/react
  and writes characterization tests for `packages/svg/src/core/*`. Plans 002,
  003, 004, 006 add or update tests using the harness 001 provides. Do not run
  002/003/004/006 before 001 or their verification gates have no runner.
- **002 subsumes the per-adapter sanitize bugs** (Vue/Svelte keeping
  `javascript:` hrefs; Vue/Svelte over-stripping safe `url(#…)` refs) by routing
  all three DOM adapters through one shared `parseInlineSvg` in core. It also
  moves Vue's per-instance cache to a shared module-level cache so the
  "in-memory caching in every adapter" claim holds.
- **003 fixes the React cache write** (the default adapter never calls
  `svgCache.set`). Independent of 002 but both touch `svg.tsx` — execute 002
  before 003 so 003's one-line add lands in the already-consolidated file.
- **004 (Flutter) has a verification gap:** no `flutter`/`dart` toolchain is
  installed in this environment. The executor should make the code + test edits
  and run `flutter test` if available; if not, report "STOPPED: no flutter
  toolchain" so the reviewer can verify by reading. Do not skip the edits.
- **006 depends on 003** because the cache fix changes whether inline-callback
  refetch hits the network; 006's regression test asserts no *effect re-run*
  (no second `onSvgLoad` call), which is only meaningful once caching is in
  place.
- **008 depends on 002, 003, 004, 006** so docs describe the fixed behavior.
- **005 and 007 are independent** and can run any time after 001 (005 needs the
  test script 001 adds).

## Findings considered and rejected

- _none yet_ — first run.
